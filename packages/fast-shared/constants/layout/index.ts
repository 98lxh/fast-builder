export const layout = {
  defaultHeaderHeight: 48, // 默认布局的header高度
  designerHeaderHeight: 38, // designer布局的header高度
  collapseSidebarWidth: 56, // 默认布局侧边栏折叠宽度
  designerCollapseSidebarWidth: 40, // designer布局sidebar折叠宽度
  sidebarWidth: 220, // 侧边栏默认宽度
  
  sidebarTransition: `width ease-in-out .3s,transform ease-in-out .3s`, // 侧边栏动画
  headerTransition: `width ease-in-out .3s`, // header动画
  mainTransition: `padding ease-in-out .3s` // 主内容动画
}
